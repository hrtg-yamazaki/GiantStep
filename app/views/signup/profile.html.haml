%h1 プロフィール入力ページ

= form_for @user, url: user_create_signup_index_path, method: :post do |f|
  = render partial: "error_messages", locals: { model: @user }

  .field
    = f.label :nickname
    %br/
    = f.text_field :nickname, autofucus: true, autocomplete: 'nickname'
    
  .field
    = f.label :gender
    %br/
    = f.select :gender, User.genders_i18n.invert, autofucus: true

  .field
    = f.label :birthday
    %br/
    != sprintf(f.date_select(:birthday, prefix: "birthday", prompt:"--", start_year: Time.now.year, end_year: 1900, use_month_numbers: true, date_separator:"%s"),"年","月") + "日"
    -# with_css_classes:'birthday-select',

  .field
    = f.label :profile
    %br/
    = f.text_area :profile, autofucus: true

  .actions
    = f.submit "登録"


